<?xml version='1.0' encoding='utf-8'?>
<tomcat-users>
{% for user, userconfig in salt['pillar.get']('tomcat:manager', {}).iteritems() %}
    <user username="{{ user }}" password="{{ userconfig['passwd'] }}" roles="{{ userconfig['roles'] }}"/>
{% endfor %}
</tomcat-users>
